node{
    stage('Clone repository'){
        checkout scm
        sh 'echo Repository cloned'
    }
    stage('Build imagage'){
        sh 'cd blog && docker-compose up --build'
    }
    stage('Test image'){
        app.inside {
            sh 'echo Test passed'
        }
    }
}