pipeline {
    agent none

    stages {
        stage ('Build') {
            agent any
                
                steps {
                    echo 'Buduje aplikacje blog.'
                    sh 'cd blog'
                    sh 'ls'
                    sh "docker-compose up --build"
                }
        }
    }
}

