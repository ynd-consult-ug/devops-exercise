pipeline {
    agent none

    stages {
        stage ('Build') {
            agent any
                
                steps {
                    echo 'Buduje aplikacje blog.'
                    sh "git clone https://github.com/JakubNajman/devops-exercise"
                    sh "docker-compose up --build -f devops-exercise/blog"
                }
        }
    }
}