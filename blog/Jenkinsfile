pipeline {
    agent none

    stages {
        stage ('Build') {
            agent any
                
                steps {
                    echo 'Buduje aplikacje blog.'
                    sh 'ls'
                    sh 'cd blog && docker-compose up --build'
                }
        }
    }
}

