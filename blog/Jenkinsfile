pipeline {
    agent none

    stages {
        stage ('Build') {
            agent any
                
                steps {
                    echo 'Buduje aplikacje blog.'
                    sh "docker build -t blog"
                }
        }
    }
}

